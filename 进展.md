  └I have configured the entire environment on your WSL system, but starting the project is currently blocked by network
   issues.

   **Configuration Status:**
   1.  **Node.js**: Installed (v18.19.1 via apt).
   2.  **Docker**: Installed and running (v28.2.2).
   3.  **Environment Variables**: Configured (`.env` files created).
   4.  **Docker Mirrors**: Configured (`/etc/docker/daemon.json`) to use faster mirrors (DaoCloud, etc.) for China access.

   **Current Issue:**
   When attempting to pull Docker images, the system reports:
   `dial tcp ... connect: network is unreachable`

   **Diagnosis:**
   Your WSL instance cannot access the external network. The warning `wsl: 检测到 localhost 代理配置，但未镜像到 WSL`
   indicates your Windows proxy/VPN settings are not being correctly applied to WSL.

   **Recommended Fixes:**
   1.  **Configure Proxy in Docker**: Edit `/etc/default/docker` to add `export
   HTTP_PROXY="http://host.docker.internal:7890"` (replace 7890 with your proxy port).
   2.  **Use Mirrored Networking (Windows 11)**: Create a `.wslconfig` file in your Windows User Profile directory
   (`%UserProfile%\.wslconfig`) with:
       ```ini
       [wsl2]
       networkingMode=mirrored
       dnsTunneling=true
       firewall=true
       autoProxy=true
       ```
       Then restart WSL: `wsl --shutdown`.
   3.  **Disconnect VPN**: Temporarily disconnect VPN/Proxy on Windows and retry `wsl -u root docker-compose up -d`.






Once prerequisites are installed, run these commands in your project root:

1.  **Start All Services (Backend, Frontend, DB, Redis)**:
    ```bash
    docker-compose up --build -d
    ```

2.  **Run Database Migrations**:
    ```bash
    docker-compose exec backend alembic upgrade head
    ```

3.  **Access the App**:
    *   Web App: [http://localhost:3000](http://localhost:3000)
    *   API Docs: [http://localhost:8000/docs](http://localhost:8000/docs)



可以这样操作，分 3 种常用场景：

停止所有容器（不删除）
docker-compose stop

继续启动已停止容器（不重新构建）
docker-compose start

# 重建前端（代码更新后）
docker compose build frontend && docker compose up -d 
docker compose up --build -d backend && docker compose up -d

# 查看日志
sudo docker compose logs -f frontend
sudo docker compose logs -f backend

# 完整重启
sudo docker compose restart

# 停止全部
sudo docker compose down
